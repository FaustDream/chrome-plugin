{
  "name": "cloudpiovt-plugin",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "云枢/氚云在线低代码平台与本地目录桥接扩展（TypeScript 按职责重写版）",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
