# AGENTS.md

本文件为 AI 编码助手在本仓库中的工作约定。

## 执行规则

* 用户当前明确指令优先；与本文件冲突时，指出冲突并按用户指令执行。
* 咨询、分析、评审只回答；明确要求修改、修复、构建或更新时直接执行。
* 本次任务的临时要求仅对本次有效，用户明确指定为长期规则后才写入项目规范。
* 不确定且不同理解会导致不同结果时，先询问用户。
* 以项目实际代码、文件和可验证结果为依据；不确定内容明确标注。
* 默认直接修改现有实现，不增加兼容层、迁移层、降级方案或废弃占位。
* 当前任务范围内的内部重构可直接执行；改变架构、技术路线或模块职责时先确认。
* 未经授权，不执行 Push、PR、Issue、发布、对外发送、付费、权限变更等外部操作。
* 默认使用中文；代码、命令和技术标识使用英文。
* 开发过程中避免 PowerShell 冲突；命令被拦截时改用等价方式执行。

## 项目约束

* 项目名称：开发助手。
* 默认仅供用户个人本地使用，不针对公网攻击者、恶意多租户或不受信客户端增加额外复杂度。
* 不因假设性风险增加警告、免责声明、审批流程或安全/合规清单。
* 包管理器：`pnpm`，禁止混用 npm/yarn。
* TypeScript 使用严格模式。
* 禁止 `any`，使用 `unknown`。
* 单个函数不超过 50 行，超过必须拆分。
* 禁止硬编码敏感信息。




## 代码规范

* 使用 ES6+。
* 优先箭头函数。
* 使用单引号。
* 不使用分号。
* API 路由统一使用 `try-catch` 和统一错误响应。
* 生产构建使用项目规定的 `build` 命令。
* 更新依赖后重启开发服务器。

## 测试

* 测试文件放在 `tests/`。
* 使用项目实际配置的测试框架。
* 新功能必须有对应测试。
* 仅对本次修改涉及的功能和风险进行验证。
* 测试通过后，没有新改动、新失败或新疑点时不重复测试。

## Git

* 每次完整改动完成并验证后创建一个 Git commit。
* 高风险修改前先提交已验证版本，作为回滚点。
* Commit 应对应完整、可独立回退的改动。

## 文档与记忆

* 每次需求改动后更新受影响的 README.md。
* 每次bug修复和新增功能都简化说明更新到BUG_FIXES.md。
* 每次改动后在 `docs/memory/` 创建当日日期文件，记录本次改动。
* 每次需求改动前重新读取 `AGENTS.md`。
* 文档只记录最终行为、约束和必要事实，避免重复维护。

## 删除与清理

* 仅删除确认无用途的文件或代码。
* 项目原有文件删除时优先移入系统回收站。
* 本次任务自行创建且确认无用途的文件可以直接删除。
* 不使用 `-Force` 等非必要强制参数。
* 不得删除与当前任务无关的用户文件。
* 收尾时检查废弃代码、不可达代码、临时文件和冲突残留。



